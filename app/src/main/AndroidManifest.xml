<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CafeManagementApplication">
        <activity
            android:name=".view.activity.CreateReservationActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".view.activity.ReservationListActivity" />
        <activity
            android:name=".view.activity.ReservationDetailActivity" />

    </application>

</manifest>
